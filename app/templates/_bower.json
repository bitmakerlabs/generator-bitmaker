{
  "name": "<%= _.slugify(appname) %>",
  "private": true,
  "dependencies": {<% if (includeBootstrap) { if (includeSass) { %>
    "bootstrap-sass-official": "~3.1.0",<% } else { %>
    "bootstrap": "~3.0.3",<% }} if (includeModernizr) { %>
    "modernizr": "~2.6.2",<% } if (includeBourbon) { %>
    "bourbon": "~3.2.1",
    "neat": "~1.5.1", <% } %>
    "jquery": "~1.11.0",
    "normalize-css": "~3.0.1"
  },
  "devDependencies": {}
}

