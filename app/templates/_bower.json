{
  "name": "<%= appFolder %>",
  "private": true,
  "dependencies": {<% if (includeBootstrap) { if (includeSass) { %>
    "bootstrap-sass": "~3",<% } else { %>
    "bootstrap": "~3",<% }} if (includeModernizr) { %>
    "modernizr": "~2.6.2",<% } if (includeBourbon) { %>
    "bourbon": "~4",
    "neat": "~1", <% } %>
    "jquery": "~1.11.0",
    "normalize-css": "~3.0.2"
  },
  "devDependencies": {}
}

