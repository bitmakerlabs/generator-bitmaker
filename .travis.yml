language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bitmaker' ]; then cd .. && eval "mv $currentfolder generator-bitmaker" && cd generator-bitmaker; fi

